import React from 'react'
import { Box, Typography } from '@mui/material'

const NotificationSettings = () => (
  <Box>
    <Typography variant="h6">NotificationSettings</Typography>
    
  </Box>
)

export default NotificationSettings
