import React from 'react'
import { Box, Typography } from '@mui/material'

const TraderHistory = () => (
  <Box>
    <Typography variant="h6">TraderHistory</Typography>
    
  </Box>
)

export default TraderHistory
