import React from 'react'
import { Box, Typography } from '@mui/material'

const TradeFilters = () => (
  <Box>
    <Typography variant="h6">TradeFilters</Typography>
    
  </Box>
)

export default TradeFilters
