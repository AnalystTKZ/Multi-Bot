import { describe, it, expect } from 'vitest'
import PositionsList from '@/components/positions/PositionsList'

describe('PositionsList', () => {
  it('is defined', () => {
    expect(PositionsList).toBeTruthy()
  })
})
